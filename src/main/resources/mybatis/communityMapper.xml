<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="community">
  	<select id="notice" parameterType="list" resultType="noticeDto">
  		SELECT 
  			notice_seq
  		  , notice_title
  		  , input_date
  		FROM notice
  		ORDER BY notice_seq DESC
  	</select>
  	
  	<select id="selectNotice" parameterType="int" resultType="noticeDto">
		SELECT
			  notice_seq
			, notice_title
			, notice_content
			, input_date
		FROM notice
		WHERE notice_seq = #{notice_seq}
	</select>
	
  	<select id="faq" parameterType="list" resultType="faqDto">
  		SELECT 
			no
		  , category
		  , title
		  , input_date
		FROM faq
		ORDER BY no DESC
  	</select>
  		
  	<select id="qna" parameterType="list" resultType="QnaDto">
		SELECT 
			no
		  , title
		  , input_date
		  , status
		FROM qna
		ORDER BY no DESC
	</select>
	
	<insert id="qnaInsert" parameterType="qnaDto">
		INSERT INTO qna(		
			no
		  , title
		  , input_date
		  , status
		) VALUES (
			 #{no}
		   , #{title}
		   , #{input_date}
		   , #{status}
		)
	</insert>
  	
  </mapper>