<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	 
	<!-- 로그인 -->
	<select id="loginCheck" resultType="UserDto" parameterType="UserDto">
		SELECT * FROM USER
		WHERE USER_ID = #{user_id} AND USER_PW = #{user_pw}
	</select>
	
	<!-- 아이디 중복 체크 -->
	<select id="idCheck" parameterType="UserDto" resultType="UserDto">
		SELECT USER_ID as user_no FROM USER
		WHERE USER_ID = #{user_id}
	</select>
	
	<!-- 회원가입 -->
	<insert id="userRegist" parameterType="UserDto">
		INSERT INTO USER
		VALUES(USER_NO_SEQ.NEXTVAL, #{user_id}, #{user_pw}, #{user_name}, #{user_email}, #{user_addr} , #{user_phone}, 'U', 'Y', {user_regdate}, #{user_upd_date} )
	</insert>
	
	<!-- 회원정보 수정 -->
	<update id="userUpdate">
		UPDATE USER SET
		USER_PW = #{user_pw}, USER_EMAIL = #{user_email}, USER_ADDR = #{user_addr}, USER_ENABLE= #{user_enable}
		WHERE USER_NO = #{user_no}
	</update>
	
	<!-- 회원탈퇴 -->
	<update id="userDelete">
		UPDATE USER SET
		USER_ENABLE = 'N'
		WHERE USER_NO = #{user_no}
	</update>
	
</mapper>